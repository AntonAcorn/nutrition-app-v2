diff --git a/node_modules/@capacitor/camera/Package.swift b/node_modules/@capacitor/camera/Package.swift
index e59ef72..a54fe10 100644
--- a/node_modules/@capacitor/camera/Package.swift
+++ b/node_modules/@capacitor/camera/Package.swift
@@ -11,7 +11,7 @@ let package = Package(
     ],
     dependencies: [
         .package(url: "https://github.com/ionic-team/capacitor-swift-pm.git", from: "8.0.0"),
-        .package(url: "https://github.com/ionic-team/ion-ios-camera.git", from: "1.0.4")
+        .package(name: "ion-ios-camera", path: "../../../ios/ion-ios-camera")
     ],
     targets: [
         .target(
diff --git a/node_modules/@capacitor/camera/ios/Sources/CameraPlugin/CameraPlugin.swift b/node_modules/@capacitor/camera/ios/Sources/CameraPlugin/CameraPlugin.swift
index 968733e..379a679 100644
--- a/node_modules/@capacitor/camera/ios/Sources/CameraPlugin/CameraPlugin.swift
+++ b/node_modules/@capacitor/camera/ios/Sources/CameraPlugin/CameraPlugin.swift
@@ -145,7 +145,8 @@ public class CameraPlugin: CAPPlugin, CAPBridgedPlugin {
 
     @objc override public func requestPermissions(_ call: CAPPluginCall) {
         // get the list of desired types, if passed
-        let typeList = call.getArray("permissions", String.self)?.compactMap({ (type) -> CameraPermissionType? in
+        // Swift 5 compat: getArray(_:) optional form also gated; use default-value form + cast
+        let typeList = (call.getArray("permissions", []) as? [String])?.compactMap({ (type) -> CameraPermissionType? in
             return CameraPermissionType(rawValue: type)
         }) ?? []
         // otherwise check everything
@@ -240,7 +241,8 @@ public class CameraPlugin: CAPPlugin, CAPBridgedPlugin {
         // Make sure they have all the necessary info.plist settings
         if let missingUsageDescription = checkUsageDescriptions() {
             CAPLog.print("⚡️ ", self.pluginId, "-", missingUsageDescription)
-            call.reject(missingUsageDescription)
+            // Swift 5 compat: call.reject gated behind $NonescapableTypes
+            call.unimplemented(missingUsageDescription)
             return
         }
 
