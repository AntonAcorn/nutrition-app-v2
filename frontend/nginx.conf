server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Hashed Vite assets (index-XXXX.js, ProfileTab-XXXX.js, etc.) — safe to
    # cache forever, content-addressed by hash.
    location /assets/ {
        access_log off;
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    # Service worker and HTML entry — must always revalidate so a fresh deploy
    # is picked up on next launch (WKWebView's URL cache otherwise pins the
    # old index.html that references chunk hashes no longer on disk).
    location = /sw.js {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        expires off;
    }

    # Universal Links (iOS) and App Links (Android) verification files. Apple
    # fetches /.well-known/apple-app-site-association expecting application/json
    # with NO file extension; Android fetches /.well-known/assetlinks.json the
    # same way. Both must be cacheable for verification to stick across reboots.
    location /.well-known/ {
        default_type application/json;
        add_header Cache-Control "public, max-age=3600";
        try_files $uri =404;
    }

    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        expires off;
    }

    # SPA fallback — everything else goes to index.html with the same no-cache
    # so deep links also re-validate.
    location / {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri $uri/ /index.html;
    }
}
